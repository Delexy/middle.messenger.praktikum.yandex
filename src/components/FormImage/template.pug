form.form.from-image(class=isModal ? 'form_modal' : '' class=attributes.class id=attributes.id)
  if isModal 
    button.form__close(type="button")
  p.form__title= title
  .auth-form__inputs
    | !{PhotoInput}
    | !{SaveBtn}
    span.form-image__error= errorText
